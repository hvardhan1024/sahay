<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="min-h-screen bg-gray-50">
  <%- include('../../partials/navbar') %>
  
  <div class="flex">
    <%- include('../../partials/sidebar') %>
    
    <!-- Main Content -->
    <div class="flex-1 p-8">
      <!-- Header -->
      <div class="mb-8">
        <h1 class="text-3xl font-bold text-gray-800 mb-2">Helper Profile</h1>
        <p class="text-gray-600">Manage your helper information and specializations.</p>
      </div>

      <!-- Messages -->
      <% if (typeof success !== 'undefined' && success) { %>
        <div class="mb-6 p-4 bg-green-50 border-l-4 border-green-400 rounded-lg">
          <div class="flex">
            <div class="flex-shrink-0">
              <svg class="h-5 w-5 text-green-400" viewBox="0 0 20 20" fill="currentColor">
                <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd" />
              </svg>
            </div>
            <div class="ml-3">
              <p class="text-sm text-green-700"><%= success %></p>
            </div>
          </div>
        </div>
      <% } %>

      <% if (typeof error !== 'undefined' && error) { %>
        <div class="mb-6 p-4 bg-red-50 border-l-4 border-red-400 rounded-lg">
          <div class="flex">
            <div class="flex-shrink-0">
              <svg class="h-5 w-5 text-red-400" viewBox="0 0 20 20" fill="currentColor">
                <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zM8.707 7.293a1 1 0 00-1.414 1.414L8.586 10l-1.293 1.293a1 1 0 101.414 1.414L10 11.414l1.293 1.293a1 1 0 001.414-1.414L11.414 10l1.293-1.293a1 1 0 00-1.414-1.414L10 8.586 8.707 7.293z" clip-rule="evenodd" />
              </svg>
            </div>
            <div class="ml-3">
              <p class="text-sm text-red-700"><%= error %></p>
            </div>
          </div>
        </div>
      <% } %>

      <!-- Profile Form -->
      <div class="bg-white rounded-xl shadow-sm border border-gray-200 p-8">
        <form method="POST" action="/helper/profile" class="space-y-6">
          <!-- Personal Information -->
          <div>
            <h3 class="text-lg font-semibold text-gray-800 mb-4">Personal Information</h3>
            <div>
              <label for="name" class="block text-sm font-medium text-gray-700 mb-2">
                Full Name
              </label>
              <input
                type="text"
                id="name"
                name="name"
                value="<%= helper.user.name %>"
                required
                class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-green-500 focus:border-transparent"
              >
            </div>
          </div>

          <!-- Helper Information -->
          <div>
            <h3 class="text-lg font-semibold text-gray-800 mb-4">Helper Information</h3>
            
            <div class="space-y-6">
              <!-- Experience -->
              <div>
                <label for="experience" class="block text-sm font-medium text-gray-700 mb-2">
                  Experience & Background
                </label>
                <textarea
                  id="experience"
                  name="experience"
                  rows="4"
                  placeholder="Describe your experience with mental health support, relevant training, or personal background that makes you a good helper..."
                  class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-green-500 focus:border-transparent"
                ><%= helper.experience || '' %></textarea>
              </div>

              <!-- Specializations -->
              <div>
                <label class="block text-sm font-medium text-gray-700 mb-2">
                  Areas of Specialization
                </label>
                <p class="text-sm text-gray-500 mb-3">
                  Select the areas where you feel most comfortable providing support:
                </p>
                <div class="grid grid-cols-1 md:grid-cols-2 gap-3">
                  <label class="flex items-center space-x-3 p-3 border border-gray-200 rounded-lg hover:bg-gray-50 cursor-pointer">
                    <input
                      type="checkbox"
                      name="specialization"
                      value="anxiety"
                      <%= helper?.specialization?.includes('anxiety') ? 'checked' : '' %>
                      class="h-4 w-4 text-green-600 focus:ring-green-500 border-gray-300 rounded"
                    >
                    <div>
                      <div class="font-medium text-gray-800">Anxiety Support</div>
                      <div class="text-sm text-gray-500">General anxiety, social anxiety, panic</div>
                    </div>
                  </label>

                  <label class="flex items-center space-x-3 p-3 border border-gray-200 rounded-lg hover:bg-gray-50 cursor-pointer">
                    <input
                      type="checkbox"
                      name="specialization"
                      value="depression"
                      <%= helper?.specialization?.includes('depression') ? 'checked' : '' %>
                      class="h-4 w-4 text-green-600 focus:ring-green-500 border-gray-300 rounded"
                    >
                    <div>
                      <div class="font-medium text-gray-800">Depression Support</div>
                      <div class="text-sm text-gray-500">Mood disorders, sadness, motivation</div>
                    </div>
                  </label>

                  <label class="flex items-center space-x-3 p-3 border border-gray-200 rounded-lg hover:bg-gray-50 cursor-pointer">
                    <input
                      type="checkbox"
                      name="specialization"
                      value="academic-stress"
                      <%= helper?.specialization?.includes('academic-stress') ? 'checked' : '' %>
                      class="h-4 w-4 text-green-600 focus:ring-green-500 border-gray-300 rounded"
                    >
                    <div>
                      <div class="font-medium text-gray-800">Academic Stress</div>
                      <div class="text-sm text-gray-500">Study pressure, exams, performance</div>
                    </div>
                  </label>

                  <label class="flex items-center space-x-3 p-3 border border-gray-200 rounded-lg hover:bg-gray-50 cursor-pointer">
                    <input
                      type="checkbox"
                      name="specialization"
                      value="social-anxiety"
                      <%= helper?.specialization?.includes('social-anxiety') ? 'checked' : '' %>
                      class="h-4 w-4 text-green-600 focus:ring-green-500 border-gray-300 rounded"
                    >
                    <div>
                      <div class="font-medium text-gray-800">Social Anxiety</div>
                      <div class="text-sm text-gray-500">Social situations, relationships</div>
                    </div>
                  </label>

                  <label class="flex items-center space-x-3 p-3 border border-gray-200 rounded-lg hover:bg-gray-50 cursor-pointer">
                    <input
                      type="checkbox"
                      name="specialization"
                      value="general-support"
                      <%= helper?.specialization?.includes('general-support') ? 'checked' : '' %>
                      class="h-4 w-4 text-green-600 focus:ring-green-500 border-gray-300 rounded"
                    >
                    <div>
                      <div class="font-medium text-gray-800">General Support</div>
                      <div class="text-sm text-gray-500">Listening, emotional support</div>
                    </div>
                  </label>
                </div>
              </div>

              <!-- Availability -->
              <div>
                <label for="availability" class="block text-sm font-medium text-gray-700 mb-2">
                  Current Availability Status
                </label>
                <select
                  id="availability"
                  name="availability"
                  class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-green-500 focus:border-transparent"
                >
                  <option value="available" <%= helper?.availability === 'available' ? 'selected' : '' %>>
                    Available - Ready to help students
                  </option>
                  <option value="busy" <%= helper?.availability === 'busy' ? 'selected' : '' %>>
                    Busy - Limited availability
                  </option>
                  <option value="offline" <%= helper?.availability === 'offline' ? 'selected' : '' %>>
                    Offline - Not available for support
                  </option>
                </select>
              </div>
            </div>
          </div>

          <!-- Account Information (Read-only) -->
          <div>
            <h3 class="text-lg font-semibold text-gray-800 mb-4">Account Information</h3>
            <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
              <div>
                <label class="block text-sm font-medium text-gray-700 mb-2">
                  Email Address
                </label>
                <div class="w-full px-4 py-3 bg-gray-50 border border-gray-300 rounded-lg text-gray-600">
                  <%= helper.user.email %>
                </div>
                <p class="text-xs text-gray-500 mt-1">Email cannot be changed</p>
              </div>

              <div>
                <label class="block text-sm font-medium text-gray-700 mb-2">
                  Helper Since
                </label>
                <div class="w-full px-4 py-3 bg-gray-50 border border-gray-300 rounded-lg text-gray-600">
                  <%= new Date(helper.createdAt).toLocaleDateString() %>
                </div>
              </div>
            </div>
          </div>

          <!-- Helper Stats -->
          <div>
            <h3 class="text-lg font-semibold text-gray-800 mb-4">Your Impact</h3>
            <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
              <div class="bg-blue-50 border border-blue-200 rounded-lg p-4">
                <div class="text-2xl font-bold text-blue-800"><%= helper.totalSessions || 0 %></div>
                <div class="text-sm text-blue-600">Total Support Sessions</div>
              </div>
              <div class="bg-green-50 border border-green-200 rounded-lg p-4">
                <div class="text-2xl font-bold text-green-800"><%= helper.rating?.toFixed(1) || '5.0' %></div>
                <div class="text-sm text-green-600">Average Rating</div>
              </div>
              <div class="bg-purple-50 border border-purple-200 rounded-lg p-4">
                <div class="text-2xl font-bold text-purple-800">
                  <%= helper.verified ? 'Verified' : 'Pending' %>
                </div>
                <div class="text-sm text-purple-600">Verification Status</div>
              </div>
            </div>
          </div>

          <!-- Save Button -->
          <div class="flex justify-end space-x-4">
            <button
              type="button"
              onclick="window.location.href='/helper/dashboard'"
              class="px-6 py-3 border border-gray-300 text-gray-700 rounded-lg hover:bg-gray-50 transition-colors font-medium"
            >
              Cancel
            </button>
            <button
              type="submit"
              class="px-6 py-3 bg-green-500 text-white rounded-lg hover:bg-green-600 transition-colors font-medium"
            >
              Save Changes
            </button>
          </div>
        </form>
      </div>

      <!-- Helper Guidelines -->
      <div class="mt-8 bg-yellow-50 border border-yellow-200 rounded-xl p-6">
        <div class="flex items-start space-x-3">
          <div class="flex-shrink-0">
            <svg class="w-5 h-5 text-yellow-600 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-2.5L13.732 4c-.77-.833-1.888-.833-2.598 0L3.196 16.5c-.77.833-.192 2.5 1.344 2.5z"></path>
            </svg>
          </div>
          <div>
            <h4 class="font-semibold text-yellow-800 mb-2">Important Responsibilities</h4>
            <ul class="text-sm text-yellow-700 space-y-1">
              <li>• Provide peer support within your comfort level and expertise</li>
              <li>• Know when to refer students to professional help</li>
              <li>• Maintain confidentiality and respect boundaries</li>
              <li>• Practice self-care and seek support when needed</li>
              <li>• Report any concerning situations to platform administrators</li>
            </ul>
          </div>
        </div>
      </div>
    </div>
  </div>
</body>
</html>